{
  "name": "terminalmarket",
  "version": "0.7.2",
  "description": "TerminalMarket CLI — marketplace for developers (client for terminalmarket.app)",
  "bin": {
    "tm": "bin/tm.js"
  },
  "type": "module",
  "keywords": [
    "cli",
    "marketplace",
    "terminal",
    "developers",
    "food",
    "subscription",
    "saas",
    "coworking"
  ],
  "author": "TerminalMarket",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/terminalmarket/cli"
  },
  "homepage": "https://terminalmarket.app",
  "scripts": {
    "bundle:cli": "esbuild bin/tm.js --bundle --platform=node --format=cjs --outfile=dist/tm.cjs",
    "build:bin": "npm run bundle:cli && pkg dist/tm.cjs --targets node18-linux-x64,node18-macos-x64,node18-macos-arm64 --output dist/tm",
    "build:bin:linux": "npm run bundle:cli && pkg dist/tm.cjs --targets node18-linux-x64 --output dist/tm",
    "build:bin:macos": "npm run bundle:cli && pkg dist/tm.cjs --targets node18-macos-x64,node18-macos-arm64 --output dist/tm",
    "build:bin:win": "npm run bundle:cli && pkg dist/tm.cjs --targets node18-win-x64 --output dist/tm",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "pkg": "^5.8.1"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "conf": "^12.0.0",
    "node-fetch": "^3.3.2",
    "open": "^9.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
